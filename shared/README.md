# Shared Folder

Use this folder for reusable, non-domain-specific code.

Suggested pattern:

- `shared/ui/`
- `shared/lib/`
- `shared/hooks/`
- `shared/types/`

Keep shared modules dependency-light to avoid coupling with feature domains.
