# Features Folder

Use this folder for domain-oriented modules.

Suggested pattern:

- `features/<domain>/api/`
- `features/<domain>/server/`
- `features/<domain>/ui/`
- `features/<domain>/types/`

Start by introducing new functionality here, then move legacy code incrementally.
